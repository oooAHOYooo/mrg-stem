# Server Configuration
HOST=0.0.0.0
PORT=8000

# Default LLM Provider Configuration
# Supported providers: ollama (local), anthropic (Claude), openai (GPT)
LLM_PROVIDER=ollama
LLM_MODEL=llama3

# API Keys (Required for OpenAI and Anthropic)
LLM_API_KEY=your_api_key_here
