# Docs (internal)

Loose documents — expense sheets, invoices, notes — kept here, separate from the public site.

*Optional:* To keep this folder from being served on the live site, add a rule in `netlify.toml` or use a `_redirects` to block `/docs/*`.
